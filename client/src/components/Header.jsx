import Nav from './Nav';

const styles = {
    header: {

    }
  };
const Header = () => {
    return (
        <header style={ styles.header }>
            <Nav />
        </header>
    );
};

export default Header;
